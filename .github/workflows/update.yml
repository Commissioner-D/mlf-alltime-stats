name: MLF Weekly Update

on:
schedule:
    - cron: '30 6 * * 2'   # Dienstag 06:30 UTC = 08:30 CEST / 07:30 CET
    - cron: '0 10 * * 4'   # Donnerstag 10:00 UTC = 12:00 CEST / 11:00 CET
  workflow_dispatch: {}

concurrency:
  group: mlf-update
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Fetch Fleaflicker
        run: python 03_fetch_fleaflicker.py

      - name: Aggregate
        run: python 05_aggregate.py

      - name: Generate HTML
        run: python 06_generate_html.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git reset --soft origin/main
          git add -A
          if git diff --staged --quiet; then
            echo "Keine Aenderungen."
          else
            git commit -m "Auto-update $(date -u +%Y-%m-%d)"
            git push origin main
          fi
